import { Button as ButtonNativeBase, Text } from "native-base";
export function Button() {
    return (React.createElement(ButtonNativeBase, null,
        React.createElement(Text, null, "Entra com o Google")));
}
